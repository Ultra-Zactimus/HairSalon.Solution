@{
  Layout = "_Layout"; 
}

<header>
  <p id="edit">@Html.ActionLink("Edit Client", "Edit", new { id = Model.ClientId })</p>
  <p id="delete">@Html.ActionLink("Delete Client", "Delete", new { id = Model.ClientId })</p>
</header>

@model Salon.Models.Client

<main>
  <h2>CLIENTS</h2>
  <hr />
  <h5>@Html.DisplayFor(model => model.ClientName)</h5>
</main>